; next-auth v4 declares a peer dependency of next >= 12.2.5, while this app is
; pinned to next@12.2.4. The combination works in practice, so we relax peer
; resolution here. This also makes `npm ci` in the Docker build tolerate it.
legacy-peer-deps=true
